<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.multiwindowapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MultiWindowApplication">
        <activity
            android:name=".AddActivity"
            android:exported="false"
            android:label="@string/app_name1_txt"/>
        <activity
            android:name=".InfoActivity"
            android:exported="false"
            android:label="@string/app_name4_txt"/>
        <activity
            android:name=".DateActivity"
            android:exported="false"
            android:label="@string/app_name3_txt"/>
        <activity
            android:name=".TimeActivity"
            android:exported="false"
            android:label="@string/app_name2_txt" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>